name: learnyor_hrm
description: Learnyor Employee & Intern Management System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.0.0
  provider: ^6.1.2
  go_router: ^14.0.0
  fl_chart: ^0.69.0
  shimmer: ^3.0.0
  google_fonts: ^6.2.1
  intl: ^0.19.0
  flutter_slidable: ^3.1.0
  uuid: ^4.4.0
  shared_preferences: ^2.2.3
  image_picker: ^1.1.2
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2
  flutter_staggered_animations: ^1.1.1
  cached_network_image: ^3.4.1
  flutter_secure_storage: ^10.0.0
  file_picker: ^11.0.2
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
